# HALO-Agent Environment Variables
# Copy to .env and fill in your actual values

# Required: OpenAI API key (for Manager policy and baseline Worker)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Anthropic API key (for future use)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ============================================================
# Qwen / vLLM Configuration (for RL experiments)
# ============================================================

# Worker backend: "openai" (default), "vllm", or "local"
# HALO_WORKER_BACKEND=vllm

# vLLM server URL (when using HALO_WORKER_BACKEND=vllm)
# HALO_VLLM_URL=http://localhost:8000/v1

# Model ID to request from vLLM
# - For base model: Qwen/Qwen2.5-3B-Instruct or Qwen3-4B-Instruct-2507
# - For LoRA adapter: qwen_bc, qwen_dpo, qwen_grpo (must match --lora-modules name)
# HALO_WORKER_MODEL=Qwen3-4B-Instruct-2507

# ============================================================
# Weights & Biases (optional, for experiment tracking)
# ============================================================
# WANDB_API_KEY=your_wandb_api_key_here
